plugins {
    id 'cpp-library'
    id 'maven-publish'
}

group = 'com.example'
version = '1.0'

//region Example
library {
    publicHeaders.from('inc')
    publicHeaders.from('includes')
}
//endregion

publishing {
    repositories {
        maven { url = '../repo' }
    }
}